Initialize cmake template for event-based-centroiding. This will mostly use c++ and python. start tailoring cmake by removing optix setups and initializing python module. create a .venv for testing install of python module. template works and
  there is not bug. you should remove things like profiling folder and tools/tests that are just needed for development of the template.
